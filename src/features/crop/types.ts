export interface AspectRatio {
  id: string;
  label: string;
  subtitle?: string;
  ratioLabel: string;
  ratio: number;
}

export interface CropPixelArea {
  x: number;
  y: number;
  width: number;
  height: number;
}
